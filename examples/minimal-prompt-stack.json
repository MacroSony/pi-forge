{
  "schemaVersion": 2,
  "type": "pi-forge.prompt-stack",
  "id": "minimal",
  "name": "Minimal Worker",
  "description": "DeepSeek Harness Minimal approximation: the exact persona, chat history, and only bash plus str_replace_editor when the optional Pi tool extension is installed. PTY, sandbox, and deployment guarantees still differ.",
  "autoActivate": false,
  "mode": "replace",
  "defaults": {
    "syntheticMessagesVisible": false,
    "unresolvedMacroPolicy": "warn"
  },
  "tools": {
    "allow": [
      "bash",
      "str_replace_editor"
    ]
  },
  "items": [
    {
      "kind": "block",
      "id": "worker-role",
      "name": "Worker Role",
      "enabled": true,
      "role": "system",
      "content": "You are a helpful software engineer assistant."
    },
    {
      "kind": "slot",
      "id": "chat-history",
      "name": "Chat History",
      "enabled": true,
      "slot": "chat-history",
      "options": {
        "includeSummaries": false
      }
    }
  ],
  "parameters": {}
}
